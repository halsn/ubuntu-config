### ubuntu下常用工具及配置

```
$ curl -fsSl https://raw.githubusercontent.com/halsn/ubuntu-config/master/setup.sh > setup.sh && chmod +x ./setup.sh
$ SSHOST=localhost SSPORT=1024 SSPASS=foo ./setup.sh
```

### WSL
```
$ bash -c "$(curl -fsSl https://raw.githubusercontent.com/halsn/ubuntu-config/master/setup.sh)"
```
